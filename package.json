{
  "name": "puppet",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tidal": "TYPE=tidal node index",
    "spotify": "TYPE=spotify node index",
    "napster": "TYPE=napster node index",
    "amazon": "TYPE=amazon node index",
    "all": "git clean -f && git pull && FILE=napsterAccount.txt RAND=true node index",
    "check": "CHECK=true node index",
    "push": "git add . && git commit -m c && git push",
    "compare": "node compare && git add napsterAccount.txt && git commit -m c && git push",
    "vpn": "expressvpn disconnect && expressvpn connect nl",
    "initn": "/sbin/ip route del 5.189.154.0/24 via 5.189.154.1 dev ens18 && /sbin/ip route add 5.189.154.0/24 dev ens18 proto kernel scope link src 5.189.154.39 && expressvpn connect nl",
    "inits": "/sbin/ip route del 5.189.185.0/24 via 5.189.185.1 dev ens18 && /sbin/ip route add 5.189.185.0/24 dev ens18 proto kernel scope link src 5.189.185.48 && expressvpn connect nl",
    "inity": "/sbin/ip route del 173.249.43.0/24 via 173.249.43.1 dev ens18 && /sbin/ip route add 173.249.43.0/24 dev ens18 proto kernel scope link src 173.249.43.6 && expressvpn connect nl",
    "addons": "apt-get install build-essential clang libdbus-1-dev libgtk-3-dev libnotify-dev libgnome-keyring-dev libgconf2-dev libasound2-dev libcap-dev libcups2-dev libxtst-dev libxss1 libnss3-dev gcc-multilib g++-multilib curl gperf bison python-dbusmock"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ajax-request": "^1.2.3",
    "fs": "0.0.1-security",
    "puppeteer": "^1.11.0",
    "puppeteer-core": "^1.11.0"
  }
}